import React from 'react';
import Connexion from '../components/Connexion';

const Home = () => {
    return (
        <div className='container'>
            {<Connexion />}
        </div>
    );
};

export default Home;